# 변경 이력

## 2026-08-18 — 실호출 검증에서 찾은 결함 수정

강의 전날 실제 API 키로 step0~step6 전 구간을 호출해 검증했습니다.
아래 다섯 건은 그 과정에서 드러난 결함이며, 모두 고치고 재검증했습니다.

### 1. 대화 이력이 안전 필터에 걸려 대화가 영구히 막히던 문제 (심각)

**증상** — 한 대화에서 "비밀번호" 같은 민감어가 한 번 오가면, 그 뒤로는 무슨 질문을 해도
"민감정보가 포함된 요청" 응답만 돌아왔습니다. 대화를 새로 시작하기 전에는 복구되지 않았습니다.

**원인** — `SafeGuardAdvisor`의 기본 order는 0이고 `MessageChatMemoryAdvisor`는
`HIGHEST_PRECEDENCE + 1000`(음수)입니다. 메모리가 먼저 실행되어 대화 이력을 프롬프트에 실은 뒤
안전 필터가 **프롬프트 전체**를 검사하므로, 이력에 남은 민감어를 매번 다시 발견했습니다.

**수정** — 어드바이저 순서를 코드에 명시했습니다.

```
모더레이션(HIGHEST_PRECEDENCE+100) → 안전 필터(+200) → 대화 메모리(+1000) → RAG → 로거
```

필터가 메모리보다 먼저 실행되므로 검사 대상은 이번 질문 하나뿐입니다.

### 2. 모더레이션 어드바이저가 실제로는 단락하지 않던 문제

**증상** — 주석에는 "LLM을 호출하지 않고 즉시 거절"이라고 적혀 있었지만, 실제로는 컨텍스트에
표시만 남기고 체인을 그대로 통과시킨 뒤 응답만 바꿔치기했습니다. 모델 호출 비용과 도구 호출이
그대로 발생했고, 차단된 질문이 대화 이력에도 남았습니다.

**수정** — `CallAdvisor`/`StreamAdvisor`를 직접 구현해 `chain.nextCall()`을 호출하지 않고
그 자리에서 거절 응답을 반환합니다. 검사 대상도 프롬프트 전체가 아니라 **마지막 사용자 메시지**로
좁혔습니다.

### 3. 요약·분류·유해성 판정이 500으로 실패하던 문제 (심각)

**증상** — `/api/summarize`, `/api/classify`, `/api/moderation/check`, `/api/safe/comments`가
모두 500을 반환했습니다.

**원인** — 단발 호출인데도 대화용 `boardChatClient`를 그대로 썼습니다.
`MessageChatMemoryAdvisor`는 conversationId를 반드시 요구하므로
`IllegalArgumentException: conversationId cannot be null`이 발생했습니다.
안전 필터도 함께 붙어 있어, 판정 대상 문장에 민감어가 들어 있으면 판정 호출 자체가 막혔습니다.

**수정** — 단발 작업용 `taskChatClient` 빈을 분리했습니다. 메모리·안전 필터·도구가 붙지 않습니다.
요약·분류·판정·순화와 `/api/chat-meta`, `/api/chat-compare`가 이 클라이언트를 씁니다.

### 4. RAG가 맞는 문단을 찾고도 걸러 내던 문제

**증상** — "제재를 3번 받으면 어떻게 되나요?"에 "모르겠습니다"라고 답했습니다.

**원인** — 한국어 문장은 `text-embedding-3-small`에서 코사인 유사도가 0.2~0.4에 몰립니다.
임계값 0.3에서 정답 문단이 잘렸습니다. 정책 문서의 제재 단계가 `- 3회: 30일간 글쓰기 제한`처럼
나열형이라 질문 문장과 어휘가 겹치지 않은 것도 겹쳤습니다.

**수정** — `similarityThreshold`를 0.2로, `topK`를 6으로 조정하고 정책 문서의 제재 단계를
완결된 문장으로 다시 썼습니다.

### 5. 거절 메시지가 영어로 나오던 문제

`SafeGuardAdvisor`의 기본 응답이 영어(`I'm unable to respond...`)였습니다.
`failureResponse`를 한국어로 지정했습니다.

### 그 밖

- OpenAI 호환 게이트웨이를 쓸 수 있도록 `base-url`을 `OPENAI_BASE_URL` 환경변수로 뺐습니다.
  기본값은 `https://api.openai.com`이라 기존 사용에는 영향이 없습니다.

## 2026-08-18 — 실습 화면 추가

KOSTA 원본에는 단계마다 `static/index.html` 확인 화면이 있었는데, 이 저장소를 새로 구성하면서 빠져 있었습니다.
게시판 도메인에 맞춰 step0부터 step6까지 다시 만들었습니다.

- 단계별로 탭이 늘어납니다. step0은 게시판만, step6은 챗봇·정책 RAG·안전 필터·자동분류·구조화 요약·게시판 여섯 탭입니다.
- 안전 필터 화면은 판정 결과를 BLOCK·REWRITE·HOLD·PASS 배지로 보여 주고, 원문과 순화문, 판정 사유를 함께 표시합니다.
- 외부 CDN을 쓰지 않고 CSS를 파일 안에 넣었습니다. 사내망에서 외부 접속이 막혀도 화면이 그대로 뜹니다.
- 실제 브라우저에서 챗봇 질의와 댓글 순화 판정까지 동작을 확인했습니다.
