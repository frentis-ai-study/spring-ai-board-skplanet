server:
  port: 8080

spring:
  application:
    name: spring-ai-board-step0

  datasource:
    url: jdbc:h2:file:./data/boarddb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
    username: board
    password: board

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: false

logging:
  level:
    com.frentis.board: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health,info
